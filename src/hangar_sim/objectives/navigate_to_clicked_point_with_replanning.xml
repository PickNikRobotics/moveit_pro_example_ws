<?xml version="1.0" encoding="UTF-8" ?>
<root
  BTCPP_format="4"
  main_tree_to_execute="Navigate to Clicked Point with Replanning"
>
  <!--//////////-->
  <BehaviorTree
    ID="Navigate to Clicked Point with Replanning"
    _description="Navigate to a point clicked by the user in the UI. This Objective will repeatedly replan the path as new obstacles appear on the local costmap."
    _favorite="true"
  >
    <Control ID="Sequence" name="TopLevelSequence">
      <!--Seeding from TF is what this Behavior does: it reads map -> ridgeback_base_link and hands
           that pose to the filter. That only ever worked at cold boot because the transform was
           coming from the simulator, and amcl was self-seeding at the spawn pose on activation.
           With set_initial_pose false the filter starts holding nothing, map -> odom does not
           exist, and this lookup has nothing to resolve through.

           So this Objective did not stop working - it stopped being CARRIED BY GROUND TRUTH, which
           is the crutch this whole capability exists to remove. The order is localize, then
           navigate, and that is the order on hardware too. The branch below turns what would
           otherwise be an opaque transform failure on the very first node into the instruction.-->
      <Control ID="Fallback" name="Re-seed from the current estimate, or say what is missing">
        <Action ID="SetInitialPose" robot_frame_id="ridgeback_base_link" />
        <Control ID="Sequence" name="The filter has no estimate to re-seed from">
          <Action
            ID="LogMessage"
            log_level="error"
            message="Cannot navigate: the particle filter has not been seeded, so map -> ridgeback_base_link does not resolve and the robot's current pose cannot be read. Run the &quot;Localize Robot&quot; Objective first, then run this one."
          />
          <Action ID="AlwaysFailure" name="Stop before planning blind" />
        </Control>
      </Control>
      <Action
        ID="CreatePoseStamped"
        reference_frame="base_link"
        pose_stamped="{start}"
      />
      <Action
        ID="SwitchController"
        activate_controllers="platform_velocity_controller_nav2"
        deactivate_controllers="velocity_force_controller;joint_trajectory_controller;platform_velocity_controller"
      />
      <Action
        ID="GetPoseFromUser"
        is_normal="true"
        pose_prompt="Click point on UI"
        user_pose="{goal}"
        view_name="Visualization"
      />
      <Action
        ID="NavigateToPoseAction"
        action_name="/navigate_to_pose"
        behavior_tree_path="/opt/ros/jazzy/share/nav2_bt_navigator/behavior_trees/navigate_to_pose_w_replanning_and_recovery.xml"
        ignore_stamp_time="true"
        pose_stamped="{goal}"
      />
      <Action
        ID="SwitchController"
        activate_controllers="joint_trajectory_controller"
      />
    </Control>
  </BehaviorTree>
  <TreeNodesModel>
    <SubTree ID="Navigate to Clicked Point with Replanning">
      <MetadataFields>
        <Metadata runnable="true" />
        <Metadata subcategory="Navigation" />
      </MetadataFields>
    </SubTree>
  </TreeNodesModel>
</root>
