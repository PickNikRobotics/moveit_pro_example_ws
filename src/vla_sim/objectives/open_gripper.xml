<?xml version="1.0" encoding="UTF-8" ?>
<root BTCPP_format="4" main_tree_to_execute="Open Gripper">
  <BehaviorTree
    ID="Open Gripper"
    _description="Open the gripper"
    _favorite="false"
  >
    <Control ID="Sequence" name="root">
      <!--
        The recorded `action` gets its gripper channel from here: a
        GripperCommand goal is not observable on any topic. Wrapped so that a
        stack running without the bridge still moves the gripper - teleoperation
        drives it through this Objective, and a recording side-channel must not
        be able to stop the robot.
      -->
      <Decorator ID="ForceSuccess">
        <Action
          ID="SetRos2Parameter"
          node_name="/joint_command_bridge"
          parameter_name="gripper_command_position"
          parameter_value="0.0"
          parameter_type="double"
          response_timeout="1.000000"
          wait_for_server_available_timeout="0.500000"
        />
      </Decorator>
      <Action
        ID="MoveGripperAction"
        gripper_command_action_name="/robotiq_gripper_controller/gripper_cmd"
        position="0.0"
        timeout="15.000000"
      />
    </Control>
  </BehaviorTree>
  <TreeNodesModel>
    <SubTree ID="Open Gripper">
      <MetadataFields>
        <Metadata runnable="true" />
        <Metadata subcategory="Grasping" />
      </MetadataFields>
    </SubTree>
  </TreeNodesModel>
</root>
