# This simulation ships no localization, so its TF tree roots at `mj_world`
# (published as identity onto `odom`), not the frontend's default `world`.
# Override so the 3D Visualizer renders the robot without a manual Fixed
# Frame change.
referenceFrame: odom
