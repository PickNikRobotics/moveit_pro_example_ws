# Joint positions the mock hardware starts at, in radians. These match the
# center of the bridge's fake sine, so the twin does not jump when it starts.
initial_positions:
  shoulder_pan: 0.0
  shoulder_lift: 0.6
  elbow_flex: -0.6
  wrist_flex: 0.0
  wrist_roll: 0.0
  gripper: 0.7
