<?xml version="1.0" encoding="UTF-8" ?>
<root BTCPP_format="4" main_tree_to_execute="Mirror SO101 Follower">
  <BehaviorTree
    ID="Mirror SO101 Follower"
    _description="Wiggle test: so101_arm_bridge drives joint_trajectory_controller for as long as this Objective runs, sweeping each joint about 0.1 rad either side of the pose the arm is measured in when it starts. On mock the twin moves with no arm plugged in; on real hardware it is a joint-by-joint check that every servo is alive and moving the right way."
    _favorite="true"
  >
    <!--
      The bridge only mirrors while these ticks keep arriving, so mirroring
      stops on its own when this Objective is stopped. That is deliberate: the
      trajectory controller has one owner at a time, and a plan and the bridge
      would otherwise fight over it. Stop this Objective before pressing Plan.
    -->
    <Control ID="Sequence" name="root">
      <Action
        ID="LogMessage"
        log_level="info"
        message="Wiggling the SO-101 follower about its current pose. Stop this Objective before planning or executing."
      />
      <Decorator ID="KeepRunningUntilFailure">
        <Control ID="Sequence" name="Keep the bridge mirroring">
          <Action
            ID="CallTriggerService"
            service_name="/so101_arm_bridge/mirror"
            response_timeout="3.0"
            wait_for_server_available_timeout="10.0"
          />
          <Action ID="WaitForDuration" delay_duration="0.2" />
        </Control>
      </Decorator>
    </Control>
  </BehaviorTree>
  <TreeNodesModel>
    <SubTree ID="Mirror SO101 Follower">
      <MetadataFields>
        <Metadata runnable="true" />
        <Metadata subcategory="SO-101" />
      </MetadataFields>
    </SubTree>
  </TreeNodesModel>
</root>
